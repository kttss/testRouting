export class CovidDay {
  active: number;
  confirmed: number;
  deaths: number;
  recovered: number;
  activeTotal: number;
  confirmedTotal: number;
  deathsTotal: number;
  recoveredTotal: number;
  date: Date;
}
